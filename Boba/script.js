
// Example JavaScript for basic interactivity
document.addEventListener('DOMContentLoaded', function() {
    // This is where you can add interactivity after the page has loaded
});

// Example function to demonstrate interaction
function sampleFunction() {
    alert('Welcome to the Boba Tea Shop!');
}

// Call the sample function when the page loads
window.onload = sampleFunction;
